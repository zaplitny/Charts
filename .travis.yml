osx_image: xcode7.3
language: objective-c

env:
  global:
    - LANG=en_US.UTF-8

cache:
  - bundler

before_install:
  - brew update
  - bash update_carthage.sh 0.16.2
  - cd Charts && carthage bootstrap

script:
  - rake test

# after_success:
#   - bash <(curl -s https://codecov.io/bash)
